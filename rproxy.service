[Unit]
Description=rproxy reverse proxy service
After=network.target

[Service]
ExecStart=/usr/local/bin/rproxy --config /etc/rproxy/config.yaml
Restart=on-failure
User=root
Group=root

[Install]
WantedBy=multi-user.target
